#!/usr/local/bin/perl

@lines;
$break = 1;
$n = 0;
print "|| SYSTEM UP ||";

sub the_loop{
	#write this code
	}

while (1) {
	$input = <STDIN>;
	chomp($input);
	if (scalar(@lines > 0){
		the_loop();
	}
	else{
		@lines[0] = @responses0;
		@lines[0][0] = $input;
		$prev = @lines[0][0];
		print @lines[0][0];
	}
}